# Ejemplo de variables de entorno (NO introducir secretos reales aquí)
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=your_db_host
DB_NAME=your_db_name
DB_PORT=3306
# Opcional: puerto de la app (Render establece PORT automáticamente)
# PORT=16468

# Copia este archivo a `.env` y reemplaza los valores con tus credenciales reales.